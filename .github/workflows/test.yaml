name: my first github action
on: [push]
jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "Hello, World!"       # batch command one after another
      - run: tree